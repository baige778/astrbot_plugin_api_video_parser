# 更新日志 (CHANGELOG)

## [v0.3.24] - 视频并发分片下载与封面/视频下载并行化

*   **视频下载提速**：文件 ≥4MB 且 CDN 支持 Range 时自动 2~8 路并发分片下载（片数按实际大小推导、每片独立超时、失败自动重试一次、不支持 Range 自动回退单连接），大视频首次下载耗时显著降低。
*   **封面下载与视频下载并行化**：非合并模式封面仍就绪即发，合并模式封面不再串行等待视频，整体发送更快。

## [v0.3.23] - 修复 `/dy登陆` 已登录状态误报

*   **【修复】已登录误报**：后端改为首页 DOM 判定登录态后，已登录时会返回 `already_logged_in` 而非二维码，插件现正确识别该状态并提示无需重复扫码，修复 `/dy登陆` 在已登录状态下误报「获取登录二维码失败：返回内容为空」。

## [v0.3.22] - 登录态失效提醒、群聊黑白名单与 Plugin Page

*   **【新特性】抖音登录态失效自动提醒**：后端兜底解析检测到登录态失效时，插件自动提示用户重新发送 `/dy登陆` 扫码登录。
*   **【新特性】Plugin Page 自定义页面**（视频解析 · 群聊黑白名单）：自动读取机器人加入的 QQ 群，可视化配置黑白名单，并实时显示抖音登录状态。
*   **【新特性】群聊黑白名单过滤**：支持黑名单/白名单模式切换、私聊消息单独开关，用于控制哪些会话可触发视频解析。
*   **【依赖】后端同步升级到 v0.0.9**：新增 `/douyin/login/logged_in` 登录态查询接口。

## [v0.3.21] - 独立解析超时 parse_timeout_ms

*   **【新特性】独立解析超时 `parse_timeout_ms`**（默认 30000ms）：仅作用于解析请求，兼容抖音风控兜底慢路径（20~60s），修复偶发 `timed out`。
*   **【依赖】后端同步升级到 v0.0.5**：抖音三级解析、Cookie 直连提速、浏览器常驻复用、video_id 级结果缓存、video_id 复用、结构化日志。
